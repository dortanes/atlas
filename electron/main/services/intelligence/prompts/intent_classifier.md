Classify this user message into one of three categories:

1. **direct** — simple OS action via shell (PowerShell) or hotkey WITHOUT looking at the screen. Examples: open/close apps ("open Chrome"), window management ("minimize this"), volume ("mute"), system info, create/delete files, or web search ("search for X").
2. **action** — requires LOOKING at the screen (screenshot) to interact with GUI. Examples: "Click the blue button", "Read the text", "Scroll down".
3. **chat** — casual conversation or questions answerable from general knowledge. No OS interaction needed. Examples: "Hello", "How are you?".

Consider context — if previous messages involved screen actions, follow-up questions likely need action too.{{context}}
Current message: "{{command}}"
